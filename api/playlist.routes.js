// Ingredient routing.
var express = require('express');
var routes = express.Router();
var mongodb = require('../config/mongo.db');
const Cd = require('../model/cd.model');
const Band = require('../model/band.model');

//routes maken

module.exports = routes;
